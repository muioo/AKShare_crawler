# 项目级规则

1. 单股分析模块 `gui/stock_analysis.py` 中“收盘价走势”和“成交量走势”折线图，只在趋势转折点显示标注，普通数据点不显示额外文字。
2. 上升转下降时，转折高点与前一个转折低点比较显示 `增长率`；下降转上升时，转折低点与前一个转折高点比较显示 `下降率`；每个转折点标注只显示 `增长率/下降率 + 对比转折点日期`；遇到 `NaN` 跳过，基准值为 `0` 时按 `0%` 处理，禁止除零异常。
3. 收盘价走势图必须在图内摘要信息框中显示 `最新收盘价`、`5日收益率`、`20日收益率`、`60日收益率`、`距60日高点回撤`，用于同时看趋势和风险。
4. 成交量走势图必须在图内摘要信息框中显示 `最新成交量`、`5日均量`、`20日均量`、`量比(现/20均)`、`5/20均量比`，用于判断放量确认还是量能不足。
5. `gui/chart_widget.py` 绘制上述折线图时必须保留 Y 轴刻度数据，且图内摘要信息框与转折点标注可以同时存在。
6. 修改单股分析走势图相关逻辑时，优先补充最小可运行测试，至少覆盖峰值转折、谷值转折、`NaN`、零值、样本不足这五类场景。
7. `gui/chart_zoom.py` 中图表拖动必须基于 Matplotlib 原生事件和像素位移计算；单股分析图表必须支持 X/Y 双轴平移，禁止再用“在已变化视图中反算当前数据坐标”的方式实现拖动，避免单股走势图晃动。
8. `gui/chart_widget.py` 中单股分析相关图表禁止调用 `tight_layout()`；必须使用固定边距为旋转日期标签、图内摘要框和转折点标注预留空间，避免布局警告和裁切。
9. 单股分析走势图在计算“前一个交易日”或“前一个转折点”前，必须先按 `日期` 升序整理数据；后一个交易日只允许用于确认转折点，禁止参与该转折点的 `增长率` / `下降率` 计算。
